# Jadidlar Admin Panel

Минималистичная и современная админ-панель для управления контентом мобильного приложения Jadidlar.

## Возможности

- 📊 Dashboard со статистикой
- 👥 Управление джадидами (CRUD)
- 📚 Управление книгами (CRUD с загрузкой файлов)
- 📝 Управление тестами (CRUD с вопросами)
- 📈 Просмотр результатов тестов

## Технологический стек

- React 18 + TypeScript
- Material UI v5 (Material Design 3)
- Firebase (Firestore + Storage)
- Vite
- React Router v6

## Установка и запуск

1. Установите зависимости:
```bash
cd web
npm install
```

2. Создайте `.env` файл на основе `.env.example`:
```bash
cp .env.example .env
```

3. Заполните `.env` файл вашими Firebase credentials

4. Запустите dev сервер:
```bash
npm run dev
```

5. Откройте браузер по адресу `http://localhost:3000`

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в директории `dist/`

## Структура проекта

```
web/
├── src/
│   ├── components/      # Переиспользуемые компоненты
│   ├── pages/          # Страницы приложения
│   ├── services/       # Firebase сервисы
│   ├── types/          # TypeScript типы
│   ├── utils/          # Утилиты
│   ├── App.tsx         # Главный компонент
│   └── main.tsx        # Entry point
├── package.json
├── tsconfig.json
└── vite.config.ts
```

## Firebase настройки

Убедитесь, что в Firebase Console:
1. Создан проект
2. Включен Firestore
3. Включен Storage
4. Настроены Security Rules для админского доступа
