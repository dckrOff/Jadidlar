# 📱 Техническое задание для мобильного приложения Jadidlar

## 📋 Обзор изменений

**Что изменилось в бэкенде:**
- ❌ Firebase Storage больше НЕ используется для хранения файлов
- ✅ Все файлы (изображения, PDF) теперь хранятся в **Google Drive**
- ✅ Firebase Firestore **остаётся без изменений** (структура данных та же)
- ✅ В Firestore хранятся **прямые ссылки** на файлы в Google Drive

**Что нужно изменить в мобильном приложении:**
- ✅ Обновить URL изображений и PDF файлов
- ✅ Убрать зависимости от Firebase Storage (если есть)
- ✅ Использовать прямые HTTP ссылки для загрузки файлов

---

## 🔄 Миграция: Firebase Storage → Google Drive

### Было (Firebase Storage):
```
gs://bucket-name/images/jadids/12345_photo.jpg
↓
https://firebasestorage.googleapis.com/v0/b/bucket-name/...
```

### Стало (Google Drive):
```
https://lh3.googleusercontent.com/d/FILE_ID
```

---

## 📊 Структура данных в Firestore (БЕЗ ИЗМЕНЕНИЙ!)

### Коллекция: `jadids`

```json
{
  "id": "auto_generated_id",
  "name": "Махмудходжа Бехбуди",
  "nameUz": "Mahmudxo'ja Behbudiy",
  "birthYear": 1875,
  "deathYear": 1919,
  "shortDescription": "Краткое описание...",
  "fullDescription": "Полное описание...",
  "imageUrl": "https://lh3.googleusercontent.com/d/1AbCdEfGhIjKlMnOpQrStUvWxYz",
  "orderIndex": 1
}
```

**⚠️ Важно:** Поле `imageUrl` теперь содержит ссылку на Google Drive, а не Firebase Storage!

---

### Коллекция: `books`

```json
{
  "id": "auto_generated_id",
  "title": "Название книги",
  "authorId": "jadid_id_reference",
  "authorName": "Имя автора",
  "publishYear": 1912,
  "description": "Описание книги...",
  "coverImageUrl": "https://lh3.googleusercontent.com/d/1AbCdEfGhIjKlMnOpQrStUvWxYz",
  "pdfUrl": "https://drive.google.com/file/d/1AbCdEfGhIjKlMnOpQrStUvWxYz/view",
  "rating": 4.5,
  "isFavorite": false,
  "orderIndex": 1
}
```

**⚠️ Важно:** 
- `coverImageUrl` - ссылка на изображение в Google Drive
- `pdfUrl` - ссылка на PDF в Google Drive

---

### Коллекция: `tests`

```json
{
  "id": "auto_generated_id",
  "title": "Название теста",
  "description": "Описание теста",
  "questionCount": 10,
  "timeLimit": 30,
  "questions": [
    {
      "id": "q_1234567890",
      "questionText": "Текст вопроса?",
      "answers": ["Вариант 1", "Вариант 2", "Вариант 3", "Вариант 4"],
      "correctAnswerIndex": 0
    }
  ]
}
```

**Без изменений** - тесты не содержат файлов.

---

## 🔗 Форматы URL файлов

### 1. Изображения (JPG, PNG, JPEG, etc)

**Формат:**
```
https://lh3.googleusercontent.com/d/{FILE_ID}
```

**Пример:**
```
https://lh3.googleusercontent.com/d/1AbCdEfGhIjKlMnOpQrStUvWxYz
```

**Использование:**
```kotlin
// Android (Kotlin + Glide)
Glide.with(context)
    .load(jadid.imageUrl)
    .placeholder(R.drawable.placeholder)
    .error(R.drawable.error_image)
    .into(imageView)

// Android (Kotlin + Coil)
imageView.load(jadid.imageUrl) {
    placeholder(R.drawable.placeholder)
    error(R.drawable.error_image)
}
```

---

### 2. PDF файлы

**Формат для бэкенда (сохранять в Firestore):**
```
https://drive.google.com/file/d/{FILE_ID}/view
```

**Формат для прямой загрузки (используется в приложении):**
```
https://drive.google.com/uc?export=download&id={FILE_ID}
```

**⚠️ Важно:** Приложение автоматически конвертирует URL из формата `/view` в формат `/uc?export=download` для прямой загрузки PDF.

---

---

## 🔧 Решение проблемы с PDF

### Проблемы
1. URL формата `/view` открывает веб-интерфейс (HTML вместо PDF)
2. URL формата `/uc?export=download` возвращает `application/octet-stream`
3. Библиотека PdfRendererView проверяет content-type и отклоняет не-PDF

### Решение: Двухэтапная загрузка
1. **Сначала скачиваем PDF в кэш** через `HttpURLConnection` (игнорируя content-type)
2. **Затем открываем локальный файл** через `initWithFile()`

### Преимущества
- ✅ Не зависит от content-type от Google Drive
- ✅ PDF кэшируется → быстрее при повторном открытии
- ✅ Работает офлайн после первой загрузки
- ✅ Надежнее (нет зависимости от сети во время просмотра)

### Реализация
```kotlin
// 1. Скачиваем в кэш
val pdfFile = DownloadUtils.downloadPdfToCache(context, url, fileName)

// 2. Открываем локальный файл
binding.pdfView.initWithFile(file = pdfFile, ...)
```

---

## 🛠️ Требуемые изменения в коде

### 1. Удалить зависимости от Firebase Storage

**Android (build.gradle):**
```gradle
// ❌ УДАЛИТЬ эту зависимость
implementation 'com.google.firebase:firebase-storage'
```

---

### 2. Обновить модели данных

**Убедитесь что модели правильно маппят поля:**

```kotlin
// Android (Kotlin)
data class Jadid(
    val id: String = "",
    val name: String = "",
    val nameUz: String = "",
    val birthYear: Int = 0,
    val deathYear: Int = 0,
    val shortDescription: String = "",
    val fullDescription: String = "",
    val imageUrl: String = "", // ← Проверьте это поле
    val orderIndex: Int = 0
)

data class Book(
    val id: String = "",
    val title: String = "",
    val authorId: String = "",
    val authorName: String = "",
    val publishYear: Int = 0,
    val description: String = "",
    val coverImageUrl: String = "", // ← Проверьте это поле
    val pdfUrl: String = "",        // ← Проверьте это поле
    val rating: Double = 0.0,
    val isFavorite: Boolean = false,
    val orderIndex: Int = 0
)
```

---

### 3. Обновить загрузку изображений

**Проверьте что используются правильные библиотеки для загрузки изображений:**

#### Android:
```kotlin
// ✅ ХОРОШО - Glide, Coil, Picasso
Glide.with(context)
    .load(imageUrl) // Прямая HTTP ссылка
    .into(imageView)

// ❌ ПЛОХО - Firebase Storage Reference
val storageRef = Firebase.storage.reference.child("path/to/image")
storageRef.downloadUrl.addOnSuccessListener { ... }
```

#### iOS:
```swift
// ✅ ХОРОШО - SDWebImage, Kingfisher
imageView.sd_setImage(with: URL(string: imageUrl))

// ❌ ПЛОХО - Firebase Storage Reference
let storageRef = Storage.storage().reference(withPath: "path/to/image")
storageRef.downloadURL { url, error in ... }
```

#### Flutter:
```dart
// ✅ ХОРОШО - Image.network, CachedNetworkImage
Image.network(imageUrl)

// ❌ ПЛОХО - Firebase Storage Reference
final ref = FirebaseStorage.instance.ref('path/to/image');
final url = await ref.getDownloadURL();
```

---

## 🧪 Тестирование

### 1. Тестовые данные

**Тестовое изображение:**
```
https://lh3.googleusercontent.com/d/1XgT8qL7mK4nN9pRqS5tU6vW7xY8zA0b
```

**Тестовый PDF (формат для Firestore):**
```
https://drive.google.com/file/d/1UvaMQj91_jRosSuSzxBZApENF_Umlk4Y/view
```

**Тестовый PDF (прямая загрузка):**
```
https://drive.google.com/uc?export=download&id=1UvaMQj91_jRosSuSzxBZApENF_Umlk4Y
```

### 2. Чек-лист тестирования

- [ ] Изображения джадидов отображаются корректно
- [ ] Обложки книг отображаются корректно
- [ ] PDF файлы открываются корректно
- [ ] Изображения кэшируются (не загружаются повторно)
- [ ] Плейсхолдеры показываются во время загрузки
- [ ] Обработка ошибок (если изображение не загрузилось)
- [ ] Работает на медленном интернете
- [ ] Работает без интернета (показывает кэш)

### 3. Тестовые сценарии

#### Сценарий 1: Просмотр списка джадидов
1. Открыть приложение
2. Перейти в список джадидов
3. ✅ Все фотографии должны загрузиться и отображаться
4. ✅ Во время загрузки показывается placeholder

#### Сценарий 2: Просмотр деталей джадида
1. Нажать на джадида из списка
2. ✅ Фотография должна отображаться в полном размере
3. ✅ Текстовая информация отображается корректно

#### Сценарий 3: Просмотр книг
1. Перейти в раздел книг
2. ✅ Обложки всех книг отображаются
3. ✅ При нажатии на обложку показываются детали книги

#### Сценарий 4: Открытие PDF
1. Открыть детали книги
2. Нажать кнопку "Читать" или "Открыть PDF"
3. ✅ PDF открывается в браузере или встроенном просмотрщике
4. ✅ PDF загружается и отображается корректно

#### Сценарий 5: Офлайн режим
1. Просмотреть несколько джадидов/книг (изображения закэшируются)
2. Отключить интернет
3. Вернуться к просмотренным ранее
4. ✅ Изображения показываются из кэша

---

## ⚠️ Важные моменты

### 1. НЕ требуется авторизация!

**Google Drive файлы публичные, авторизация НЕ нужна:**
```kotlin
// ❌ НЕ НУЖНО
GoogleSignIn.getClient(context, ...)

// ✅ ПРОСТО загружайте по URL
Glide.with(context).load(imageUrl).into(imageView)
```

### 2. Обработка ошибок

**Обязательно обрабатывайте ошибки загрузки:**
```kotlin
// Android (Glide)
Glide.with(context)
    .load(imageUrl)
    .placeholder(R.drawable.placeholder) // Во время загрузки
    .error(R.drawable.error_image)       // При ошибке
    .into(imageView)
```

### 3. HTTPS обязательно

**Все URL используют HTTPS:**
- ✅ `https://lh3.googleusercontent.com/...`
- ✅ `https://drive.google.com/...`
- ❌ НЕ используйте `http://` (не будет работать)

---

## ✅ Чек-лист для разработчика

- [ ] Удалены зависимости Firebase Storage
- [ ] Обновлены модели данных (проверены поля imageUrl, coverImageUrl, pdfUrl)
- [ ] Используются библиотеки для загрузки изображений (Glide/Coil/SDWebImage/etc)
- [ ] Добавлены placeholder'ы для изображений
- [ ] Добавлена обработка ошибок загрузки
- [ ] Реализовано кэширование изображений
- [ ] Реализовано открытие PDF файлов
- [ ] Протестированы все сценарии
- [ ] Протестирован офлайн режим
- [ ] Проверена работа на реальных данных

---

## 📚 Дополнительные материалы


**API Firebase Firestore** (без изменений):
- Документация: https://firebase.google.com/docs/firestore
- Коллекции: `jadids`, `books`, `tests`, `test_results`
- Методы: `getDocs()`, `getDoc()`, `query()`, `orderBy()`

---

**Удачи с миграцией!**

